name: Build & Publish Clang/LLVM Base Images

on:
  push:
    branches:
      - main
  workflow_dispatch: {}

env:
  REGISTRY: ghcr.io


jobs:
  build-matrix:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    strategy:
      matrix:
        include:
          - clang: 15
            ubuntu: jammy
          - clang: 16
            ubuntu: jammy
          - clang: 17
            ubuntu: noble
          - clang: 18
            ubuntu: noble
          - clang: 19
            ubuntu: noble
      fail-fast: false

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Compute image name (lowercase)
        id: meta
        shell: bash
        run: |
          echo "image_name=${GITHUB_REPOSITORY,,}/clang-base" >> "$GITHUB_OUTPUT"

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push clang-base image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./.devcontainer/Dockerfile
          target: clang-base
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ steps.meta.outputs.image_name }}:clang-${{ matrix.clang }}-${{ matrix.ubuntu }}
            ${{ matrix.clang == 18 && matrix.ubuntu == 'noble' && format('{0}/{1}:latest', env.REGISTRY, steps.meta.outputs.image_name) || '' }}
          build-args: |
            CLANG_VERSION=${{ matrix.clang }}
            UBUNTU_VERSION=${{ matrix.ubuntu }}
